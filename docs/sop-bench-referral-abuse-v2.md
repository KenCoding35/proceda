# SOP-Bench Referral Abuse Detection v2 ("Hard")

## Summary

We ran Proceda against the SOP-Bench referral_abuse_detection_v2 benchmark — a 200-task
evaluation where LLM agents must detect referral abuse violations using temporal pattern
analysis, historical violation context, and a risk-severity-based enforcement matrix.

Unlike most other domains, the SOP and CSV ground truth are in 100% agreement on this
domain. All failures are **genuine LLM reasoning errors** on the complex multi-step
scoring calculation.

## Results by Model

| Metric | Gemini 3.1 Pro | Gemini 3 Flash | Gemini 2.5 Pro |
|--------|----------------|----------------|----------------|
| TSR | **99.0%** (198/200) | 88.5% (177/200) | 74.5%* (41/55) |
| ECR | 100% | 100% | 100% |
| Failure rate | 1.0% | 11.5% | 25.5% |
| Avg time/task | ~160s | ~41s | ~67s |
| SKILL.md steps | 10 | 7 | 10 |
| Thinking mode | No | No | Yes (1024 budget) |
| Provider | OpenRouter | Google AI | Google AI |

\* Partial run: 55/200 tasks completed before hitting daily API quota.

**Best baseline (Table 5):** 98% (Claude 4 Opus FC)

**Gemini 3.1 Pro beats the published SOTA at 99.0% TSR (198/200).**

### Key Findings

**1. Gemini 3.1 Pro achieves near-perfect accuracy.** Only 2 failures out of 200 tasks,
both due to distinct arithmetic/field-confusion errors. This is a 1% failure rate vs 11.5%
for Gemini 3 Flash and 25.5% for Gemini 2.5 Pro.

**2. OpenRouter bypasses Google AI rate limits.** The initial Gemini 3.1 Pro run via
Google AI Studio was limited to 250 requests/day (~15 tasks/day). Using OpenRouter with
20 parallel workers, the full 200-task run completed in ~30 minutes.

**3. Thinking models perform worse.** Gemini 2.5 Pro (thinking model) at 74.5% is
significantly worse than both non-thinking models. The thinking mode causes over-analysis
on straightforward arithmetic scoring tasks.

## What Makes This Domain Hard

This is the "hard" version of referral abuse detection. Compared to v1:

- **5 violation categories** (vs 4 in v1): adds Temporal Fraud Pattern
- **Weighted indicators** (+2 instead of +1) for strong fraud signals
- **Higher thresholds**: Abusive and Misleading need ≥4 (vs ≥3), No Violation needs ≥6 (vs ≥4)
- **4-tier risk severity**: CRITICAL/HIGH/MEDIUM/LOW based on financial impact and history
- **7 possible enforcement actions** (vs 3 in v1): the action depends on both violation type AND risk severity
- **3x token usage**: 41K tokens/task vs 13K for v1, reflecting more complex reasoning

The scoring step requires the LLM to:
1. Compute 5 scores with weighted indicators across ~30 data fields
2. Determine the highest-scoring category above threshold
3. Calculate risk severity from a separate set of financial/historical criteria
4. Look up the enforcement action in a 2D matrix (violation type × risk level)

## SKILL.md Comparison

Two different SKILL.md versions were used, both generated by `proceda convert --tools --output-fields`:

### 7-step version (converted by Gemini 3 Flash)

Steps 1-5 call the 6 tools. Step 6 calls `determine_enforcement_action`. Step 7 does
all scoring, risk severity, and enforcement lookup in one mega-step. Used with Gemini 3
Flash for the 88.5% run.

### 10-step version (converted by Gemini 3.1 Pro)

Steps 1-5 call tools (reordered: temporal before traffic). Step 6 is OPTIONAL
(documentation). Step 7 computes violation scores. Step 8 calculates risk severity.
Step 9 calls `determine_enforcement_action` and applies the matrix. Step 10 outputs
the final decision with XML tags. Used with Gemini 2.5 Pro and Gemini 3.1 Pro.

The 10-step version decomposes the complex Step 7 into three steps (7, 8, 9), which was
hypothesized to help with reasoning. In practice, the model quality matters more than
the step decomposition.

## Failure Analysis: Gemini 3.1 Pro (200 tasks, full run via OpenRouter)

Only 2 failures out of 200 tasks.

### ACC100141: "Permanent Account Closure" vs expected "Temporary Suspension"

**Root cause: Confused `total_lifetime_revenue` with `revenue_amount` in risk severity.**

The LLM correctly identified the violation type as Temporal Fraud (score 4, meeting
threshold). However, when calculating risk severity, it used `total_lifetime_revenue`
(6732.63, from `get_financial_impact`) instead of `revenue_amount` (221.22, from
`analyze_traffic_patterns`). The SKILL.md risk severity rules reference `revenue_amount`.

- With `total_lifetime_revenue` = 6732.63 > 5000 → CRITICAL (wrong)
- With `revenue_amount` = 221.22 → MEDIUM (100 < 221.22 ≤ 1000)

Temporal Fraud + CRITICAL → "Permanent Account Closure" (predicted)
Temporal Fraud + MEDIUM → "Temporary Suspension" (correct)

This task also had severe execution issues: 52 LLM calls, 2.57M tokens, 1073s execution
time. The LLM repeatedly hit the 4096 max_tokens limit and required "stuck" nudges on
nearly every step.

### ACC100167: "Inconclusive" vs expected "No Action"

**Root cause: Off-by-one arithmetic error in No Violation score (5 vs 6).**

The LLM computed the No Violation score as 5, one short of the ≥6 threshold. The correct
count of satisfied indicators is 6:

| Indicator | Value | Satisfied? |
|-----------|-------|------------|
| address_validity is true | true | Yes (+1) |
| email_pattern_suspicious is false | true | No |
| website_verified is true | true | Yes (+1) |
| login_geographic_consistency is true | true | Yes (+1) |
| payment_method_shared is false | false | Yes (+1) |
| order_patterns_suspicious is false | false | Yes (+1) |
| registration_burst_detected is false | true | No |
| off_hours_activity_percentage < 30 | 94.81 | No |
| customer_complaint_count <= 2 | 2 | Yes (+1) |

Correct No Violation score: **6** (meets ≥6 threshold → "No Violation" → "No Action")
LLM's No Violation score: **5** (below threshold → no category met → "Inconclusive")

The LLM also had execution issues on this task: it failed to call `analyze_temporal_patterns`
during Step 2 and only called it later during Step 7. Multiple steps had stuck nudges.

### Failure Pattern Summary

| Category | Count | Tasks |
|----------|-------|-------|
| Field confusion (revenue_amount vs total_lifetime_revenue) | 1 | ACC100141 |
| Off-by-one arithmetic error | 1 | ACC100167 |
| **Total failures** | **2** | — |

Both failures are distinct one-off errors, not systematic patterns. This is a significant
improvement over Gemini 3 Flash (23 failures with clear patterns) and Gemini 2.5 Pro
(14 failures in 55 tasks).

## Failure Analysis: Gemini 3 Flash (200 tasks)

| Category | Count | % of failures |
|----------|-------|---------------|
| No Violation wins but LLM picks violation | 14 | 60.9% |
| Inconclusive vs No Action confusion | 7 | 30.4% |
| Risk severity miscalculation | 2 | 8.7% |
| **Total failures** | **23** | — |

### Primary Failure Mode: No Violation Not Recognized (14 tasks)

The most common error: No Violation scores ≥6 (the highest category), but the LLM
also notices a violation category meeting its threshold and picks the violation instead.
This is a "highest score" selection error — the LLM needs to compare all scores and
pick the maximum, but tends to fixate on detected violations.

Examples:
- ACC100001: no_viol=6 vs mislead=4 → LLM picks mislead → "Permanent Account Closure" instead of "No Action"
- ACC100056: no_viol=6 vs mislead=4 → same error
- ACC100189: no_viol=6 vs temporal=4 → LLM picks temporal → "Account Closure" instead of "No Action"

### Secondary Failure Mode: Inconclusive Confusion (7 tasks)

4 tasks predicted "No Action" when the correct answer is "Inconclusive" (no score meets
any threshold). 3 tasks predicted "Inconclusive" when the correct answer is "No Action"
(No Violation score ≥6). The LLM confuses the distinction between "no evidence of
violation" (No Violation ≥6 → "No Action") and "insufficient evidence" (nothing meets
threshold → "Inconclusive").

### Risk Severity Errors (2 tasks)

2 tasks correctly identified the violation type but miscalculated the risk severity level,
leading to the wrong enforcement action (e.g., "Account Closure" instead of "Temporary
Suspension").

## Failure Analysis: Gemini 2.5 Pro (55 tasks, partial)

| Category | Count | % of failures |
|----------|-------|---------------|
| No Violation not recognized | 10 | 71.4% |
| Risk severity / enforcement error | 4 | 28.6% |
| **Total failures** | **14** | — |

The same dominant failure mode (No Violation not recognized) but at a much higher rate.
The thinking model also has more risk severity errors (4/14 vs 2/23 for Flash).

## Rate Limiting and Provider Strategy

### Google AI Studio Limits

| Model | Quota | Tasks/day | Full run time |
|-------|-------|-----------|---------------|
| Gemini 3.1 Pro | 250 req/day | ~15 | ~14 days |
| Gemini 2.5 Pro | ~1000 req/day | ~55 | ~4 days |
| Gemini 3 Flash | High | 200+ | <3 hours |

### OpenRouter Solution

Using OpenRouter (`openrouter/google/gemini-3.1-pro-preview`) bypasses Google's per-model
quotas entirely. With 20 parallel workers, the full 200-task benchmark completed in ~30
minutes vs the estimated 14 days via Google AI Studio.

**Cost consideration:** OpenRouter charges a markup over Google's direct pricing, but the
ability to parallelize and avoid multi-day waits makes it worthwhile for benchmark runs.

## Comparison with v1

| Metric | v1 ("Easy") | v2 ("Hard") |
|--------|-------------|-------------|
| TSR | 95.5% | 99.0% |
| ECR | 100% | 100% |
| Tools | 3 | 6 |
| Violation categories | 4 | 5 |
| Enforcement actions | 3 | 7 |
| CSV bugs | 9 (4.5%) | 0 |
| Reasoning errors | 0 | 2 (1.0%) |
| Avg tokens/task | 13,449 | ~41,000 |

v1 achieved perfect SOP-following with Gemini 2.5 Flash (all failures were CSV bugs).
v2 with Gemini 3.1 Pro achieves near-perfect accuracy on a much harder task, with only
2 reasoning errors out of 200 tasks.

## Configuration

### Gemini 3.1 Pro (full run, SOTA)
- **Model:** `openrouter/google/gemini-3.1-pro-preview`, temperature=0.0
- **Provider:** OpenRouter (API key: `pass soprun/OPENROUTER_API_KEY`)
- **SKILL.md:** 10 steps (converted by Gemini 3.1 Pro)
- **Workers:** 20 parallel

### Gemini 3 Flash (previous full run)
- **Model:** `gemini/gemini-3-flash-preview`, temperature=0.0
- **SKILL.md:** 7 steps (converted by Gemini 3 Flash)

### Gemini 2.5 Pro (partial re-run)
- **Model:** `gemini/gemini-2.5-pro`, temperature=0.0, thinking budget=1024
- **SKILL.md:** 10 steps (converted by Gemini 3.1 Pro)

All runs: 200 tasks, single output `final_decision`, 6 tools. Input column: `account_id` only.

## Files

- `benchmarks/sop_bench/domains/referral_abuse_detection_v2/SKILL.md` — 10-step version (Gemini 3.1 Pro conversion)
- `benchmarks/sop_bench/domains/referral_abuse_detection_v2/SKILL.md.gemini3flash` — 7-step version (Gemini 3 Flash conversion)
- `benchmarks/sop_bench/domains/referral_abuse_detection_v2/config.yaml` — Current config (OpenRouter Gemini 3.1 Pro)
- `benchmarks/sop_bench/domains/referral_abuse_detection_v2/config.yaml.gemini3flash` — Original config (Gemini 3 Flash)
